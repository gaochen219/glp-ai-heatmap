.DS_Store
*.log
.env
.env.local
node_modules/
.vscode/
.idea/

# DataHub interface spec (internal Feishu export, not for git)
*.docx

# KPI 源数据表（内部 Excel，不入 git）
*.xlsx
~$*.xlsx

# SQLite 本地数据库（含业务数据，不入 git）
data/*.db
data/*.db-wal
data/*.db-shm

# launchd 日志
logs/

# 旧方案归档（git history 已保留；此处本地保留一份副本备查）
.archive/

# DataHub 接口验证产物（真实返回数据，不入 git）
fixtures/
